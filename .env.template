# UniFi Controller Stack (.env)
# Copy to .env and customize. The install.sh script will generate .env for you.

# Which local IP should Docker bind to? (0.0.0.0 = all interfaces)
BIND_IP=0.0.0.0

# Linux UID/GID that should own files inside linuxserver/unifi container
PUID=1000
PGID=1000

# Timezone
TZ=America/Chicago

# Images (pin versions for reproducibility)
UNIFI_IMAGE_TAG=10.0.162-ls113
MONGO_IMAGE_TAG=8

# Mongo init + app credentials (UniFi uses three DBs)
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=unifi

MONGO_USER=unifi
MONGO_PASS=unifi

MONGO_DBNAME=unifi
MONGO_DB_STAT=unifi_stat
MONGO_DB_AUDIT=unifi_audit
MONGO_AUTHSOURCE=admin

# Ports (host side)
UNIFI_HTTPS_PORT=8443
UNIFI_INFORM_PORT=8080
UNIFI_STUN_PORT=3478
UNIFI_DISCOVERY_PORT=10001
UNIFI_GUEST_HTTPS_PORT=8843
UNIFI_GUEST_HTTP_PORT=8880
